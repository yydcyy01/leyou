server:
  port: 8083
spring:
  application:
    name: search-service
  data:
    elasticsearch:
      cluster-name: elasticsearch
      cluster-nodes: 10.211.55.4:9300
eureka:
  # 默认地址
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
  # 实例心跳时间
  instance:
    lease-expiration-duration-in-seconds: 15
    lease-renewal-interval-in-seconds: 5看